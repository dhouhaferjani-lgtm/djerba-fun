# i18n Audit Report

**Date:** 2025-12-17
**Status:** 🟡 In Progress
**Scope:** Frontend components in `apps/web/src/components/`

## Summary

**Total Components:** 68
**Using Translations:** 42 (62%)
**Needs i18n Work:** 26 (38%)

---

## Critical Issues (Homepage Components)

### 1. CategoriesGridSection.tsx

**File:** `apps/web/src/components/home/CategoriesGridSection.tsx`
**Status:** 🔴 Has `useTranslations` but doesn't use it fully
**Issues:**

- Line 46: "Explore by Activity" → should be `t('categories_title')`
- Line 49: "From desert marathons..." → should be `t('categories_subtitle')`
- Lines 10, 16, 22, 28: Category names hardcoded → should use `t('category_name_' + id)`
- Line 78: "Package" / "Packages" → should be `t('package')` / `t('packages')`

**Also Found:**

- Line 42: `bg-[#f5f0d1]` → should be `bg-accent` (color token issue)

### 2. DestinationsBentoGrid.tsx

**File:** `apps/web/src/components/home/DestinationsBentoGrid.tsx`
**Status:** 🔴 Has `useTranslations` but doesn't use it fully
**Issues:**

- Line 42: `bg-[#f5f0d1]` → should be `bg-accent` (CRITICAL: hard-coded color)
- Line 46: "Discover Tunisia" → should be `t('destinations_title')`
- Line 49: "From ancient oases..." → should be `t('destinations_subtitle')`
- Destination names are just location names, probably OK as-is

### 3. PromoBannerSection.tsx

**File:** `apps/web/src/components/home/PromoBannerSection.tsx`
**Status:** 🔴 Has `useTranslations` but hardcoded buttons
**Issues:**

- Line 58: "Learn More" → should be `t('learn_more')`
- Line 68: "Register Now" → should be `t('register_now')`
- Note: Main content ("Ultra Mirage Marathon 2025") is OK since it's event-specific

### 4. HeroSection.tsx

**File:** `apps/web/src/components/organisms/HeroSection.tsx`
**Status:** 🔴 Critical - Travel tip hardcoded
**Issues:**

- Line 50: "Travel Tip: Best time to visit the Sahara is October to April" → should be `t('hero_travel_tip')`

### 5. BlogSection.tsx

**File:** `apps/web/src/components/home/BlogSection.tsx`
**Status:** 🟡 Fallback data with hardcoded strings
**Issues:**

- Hardcoded blog post data (titles, excerpts) should come from CMS or translation files
- This is acceptable as fallback data, but should be documented

### 6. NewsletterSection.tsx

**File:** `apps/web/src/components/home/NewsletterSection.tsx`
**Status:** ⚠️ Needs review

---

## Translation Keys Needed

Add these keys to `apps/web/messages/en.json` and `apps/web/messages/fr.json`:

### Home Page Keys

```json
{
  "home": {
    "categories_title": "Explore by Activity",
    "categories_subtitle": "From desert marathons to cultural immersions, find your perfect adventure",
    "category_name_trail-running": "Trail Running",
    "category_name_hiking": "Hiking & Trekking",
    "category_name_cycling": "Cycling Tours",
    "category_name_cultural": "Cultural Tours",
    "package": "Package",
    "packages": "Packages",

    "destinations_title": "Discover Tunisia",
    "destinations_subtitle": "From ancient oases to stunning coastlines, explore the diversity of our destinations",

    "hero_travel_tip": "Travel Tip: Best time to visit the Sahara is October to April",

    "learn_more": "Learn More",
    "register_now": "Register Now"
  }
}
```

---

## Low Priority Issues

### Components Without `useTranslations`

These components may have hardcoded text but are lower priority:

1. `src/components/organisms/ListingGrid.tsx` - probably just layout
2. `src/components/organisms/LocaleSwitcher.tsx` - language names (OK hardcoded)
3. `src/components/molecules/*` - many small components
4. `src/components/atoms/*` - primitives (OK without i18n)

---

## Recommendations

### Phase 3.2: Immediate Fixes

1. **Fix DestinationsBentoGrid color** (CRITICAL)
   - Replace `bg-[#f5f0d1]` with `bg-accent`

2. **Add translation keys to en.json and fr.json**
   - Add all keys listed above

3. **Update components to use translation keys**
   - CategoriesGridSection.tsx
   - DestinationsBentoGrid.tsx
   - PromoBannerSection.tsx
   - HeroSection.tsx

### Phase 3.3: CMS Integration

Verify these sections can be managed from Filament CMS:

- ✅ Hero section (via HOME page flexible content)
- ✅ Blog section (via BlogPost resource)
- ❓ Categories grid (needs CategoriesGridBlock - already exists!)
- ❓ Destinations bento (needs custom block?)
- ❓ Promo banner (PromoBannerBlock exists!)

### Phase 3.4: Documentation

Document which data is:

- **CMS-managed** (editable in Filament)
- **Translation-managed** (in en.json/fr.json)
- **Fallback data** (hardcoded in components as last resort)

---

## Exclusions

Per the Exclusion Zone in design-system-hardening.md, the following are **NOT** audited:

- `apps/web/src/components/booking/*` (active development)
- `apps/web/src/app/[locale]/checkout/*` (active development)

---

## Next Steps

1. ✅ Create this audit document
2. ⏸️ Fix critical hard-coded color in DestinationsBentoGrid
3. ⏸️ Add translation keys to message files
4. ⏸️ Update components to use translation keys
5. ⏸️ Verify CMS coverage
6. ⏸️ Document fallback strategy
